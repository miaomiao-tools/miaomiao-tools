# 验证范围

[English](VALIDATION.en.md) · [构建说明](BUILD.md)

版本：1.2.0-experimental.1。目标：Windows x64。界面语言：中文。

- 104 项检测逻辑、安全和隔离存储检查通过。
- 32 项迷你模式检查通过，覆盖共享状态、窗口模式、布局约束与模拟显示。
- 自动检查使用本机回环服务和独立临时目录；不包含真实 AI 请求、账号验证或付费模型调用。
- 实际 GUI 验收通过：新空目录首次正常启动，无个人数据，自动巡检关闭；完整 → 迷你 → 完整切换正常。
- 禁用全部 12 个公开端点，仅启用本机 `127.0.0.1` 测试服务。F5 轻量检查产生 1 次 TCP、0 次 HTTP；单项手动 HTTP 后累计 2 次 TCP、1 次 HTTP。
- 重复手动 HTTP，以及正常退出重开后再重复 HTTP，均被冷却保护阻止，计数仍为 2 / 1。正常重开恢复迷你模式、最近历史和冷却，自动巡检始终关闭。
- 真实首次启动迷你截图随包提供，尚未进行任何检测。完整面板截图受到验收环境多显示器捕获范围限制，因此不宣称完整跨显示器与缩放布局已全面通过。
- 源码 ZIP 在独立目录重新构建，EXE 与发布文件逐字节一致；打包文件与图片链接均按明确清单校验。

不宣称已验证所有显示器排列、DPI 组合、代理软件或网络出口，也不宣称检测成功等同于 Codex / agent 可完成任务。原始诊断文件可能包含测试环境细节，故不随公开包分发。
